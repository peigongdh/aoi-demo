{
  "name": "aoi-demo-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8101",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 8102"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest"
  }
}
